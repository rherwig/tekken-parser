version: '3.8'

services:
    database:
        build: infrastructure/database
        container_name: tekken-database
        restart: "unless-stopped"
        ports:
            - "27017:27017"
        volumes:
            - ./var/data:/data/db
